<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: 'Roboto', Arial, sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(120deg, #f6d365, #fda085);
            color: #333;
            line-height: 1.6;
        }

        header {
            background: linear-gradient(90deg, #667eea, #764ba2);
            color: white;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        header h1 {
            font-size: 3rem;
            margin: 0;
            letter-spacing: 2px;
            font-weight: bold;
        }

        section {
            padding: 2.5rem;
            max-width: 1000px;
            margin: 4rem auto;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            text-align: center;
        }

        section h2 {
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            color: #2c3e50;
        }

        section p {
            font-size: 1.2rem;
            color: #444;
            line-height: 1.8;
        }

        .appointment-card,
        .offering-card,
        .testimonial-card {
            border: 1px solid #eaeaea;
            padding: 1.5rem;
            border-radius: 10px;
            margin: 1.5rem 0;
            background-color: #fff;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .appointment-card:hover,
        .offering-card:hover,
        .testimonial-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 12px 18px rgba(0, 0, 0, 0.3);
        }

        .unavailable {
            opacity: 0.8;
            background-color: #ffeded;
        }

        .unavailable-text {
            color: #d9534f;
            font-weight: bold;
            font-size: 1.1rem;
        }

        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .testimonial-card {
            background-color: #f4f4f4;
            padding: 1.5rem;
            border-radius: 12px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .testimonial-card:hover {
            transform: scale(1.1);
            box-shadow: 0 14px 20px rgba(0, 0, 0, 0.3);
        }

        footer {
            background-color: #222;
            color: white;
            text-align: center;
            padding: 1.5rem;
            margin-top: 3rem;
            font-size: 1rem;
            letter-spacing: 1px;
            box-shadow: 0 -4px 6px rgba(0, 0, 0, 0.2);
        }
    </style>
</head>
<body>
    <header>
        <h1>Welcome to Our Clinic</h1>
    </header>
    <section>
        <h2>Meet Our Professionals</h2>
        <p>Discover the best dermatologists and cosmetic experts dedicated to your well-being.</p>
    </section>
    <footer>
        <p>&copy; 2025 Welcome to Our Clinic. All rights reserved.</p>
    </footer>
</body>
</html>
